{
  "name": "backstage-mini",
  "private": true,
  "workspaces": [],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@backstage/backend-common": "^1.25.0",
    "@backstage/config": "^1.2.0",
    "@backstage/plugin-apidocs": "^0.10.0",
    "@backstage/plugin-app-backend": "^0.5.10",
    "@backstage/plugin-catalog": "^1.18.0",
    "@backstage/plugin-catalog-backend": "^1.22.0",
    "@backstage/plugin-catalog-graph": "^0.4.33",
    "@backstage/plugin-kubernetes": "^0.12.0",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "winston": "^3.13.0"
  },
  "scripts": {
    "build:backend": "node -e \"require('fs').mkdirSync('packages/backend/dist',{recursive:true});\"",
    "start": "node packages/backend --config app-config.yaml --config app-config.local.yaml",
    "tsc": "echo 'skip tsc in mini layout'"
  },
  "version": "0.0.0"
}
